// - จงเขียนฟังก์ชัน deleteTask(id) เพื่อลบ element ใน tasks ที่มี property ชื่อ id เท่ากับ id
// - execute deleteTask(1)

// ```js
// const tasks = [
//   { id: 1, name: 'Fishing' },
//   { id: 2, name: 'Shopping' },
//   { id: 3, name: 'Swimming' }
// ];

// ```

const tasks = [
  { id: 1, name: "Fishing" },
  { id: 2, name: "Shopping" },
  { id: 3, name: "Swimming" },
];

function deleteTask(id) {
  let foundindex = tasks.findIndex((item) => (item.id = id));
  if (foundindex !== -1) {
    tasks.splice(foundindex, 1);
  }
}

deleteTask(2);
console.log(tasks);
deleteTask(3);
console.log(tasks);'


let const = [fagk,mfgag,erwkopgkvm,dajklfgjioldasjiogj 
    dialog new Name fsddagjfkljklxznmklv
    
    function deleteTqaks (id){
        let foundIndex =sadfgjkl;gjklaengjkjanejktgh 9iouwehuiofgh'
