// จงเรียงข้อมูลใน arr โดยเรียงตามค่า element ยกกำลัง 2 จากน้อยไปหามาก

// ```js
// const arr = [-3, 2, 0, -7, 4, 6];
// ```

const arr = [-3, 2, 0, -7, 4, 6];
arr.sort((a, b) => (a ** 2 > b ** 2 ? 1 : -1));
console.log(arr.sort((a, b) => (a ** 2 > b ** 2 ? 1 : -1)));
